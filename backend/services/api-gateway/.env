# api
GIN_MODE=debug
HTTP_PORT=8080
JWT_ACCESS_TOKEN_EXPIRE_TIME=604800
JWT_REFRESH_TOKEN_EXPIRE_TIME=604800
# request timeout (seconds) used for context.WithTimeout in handlers and middleware
# Possible values: any integer number of seconds (e.g. 1, 3, 5, 10). Default: 3
REQUEST_TIMEOUT_SECONDS=3
